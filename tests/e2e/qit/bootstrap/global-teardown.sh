#!/usr/bin/env bash
#
# QIT globalTeardown phase — runs inside the Docker container.
# Minimal cleanup; the main teardown is handled by Playwright's global-teardown.js on the host.

echo "=== QIT Global Teardown: Complete ==="
