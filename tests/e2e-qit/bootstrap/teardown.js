/**
 * Isolated teardown that runs after your plugin's tests.
 * Use it to:
 * - Reset UI settings that can't be done via CLI
 * - Clear plugin-specific browser storage
 * - Clean up any UI-generated test data
 * - Reset plugin state via UI interactions
 */
