/**
 * Shared teardown that runs after all plugins' tests are complete.
 * Use it to:
 * - Reset shared UI settings
 * - Clear shared browser storage
 * - Clean up any shared test data via UI
 * - Reset site-wide settings that require UI interaction
 */
