import React from 'react';
import BaseIcon from '../styles/base-icon';
import icon from './icon.svg';

const PaymentRequestIcon = ( props ) => <BaseIcon { ...props } src={ icon } />;

export default PaymentRequestIcon;
